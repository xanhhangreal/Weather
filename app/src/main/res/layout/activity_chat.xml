<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/chat_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/cloud_background"
    android:padding="0dp">

    <!-- ================================
         1. Khu vực hiển thị kết quả chat (TextView)
         Đặt cao lên trên, chiếm hết không gian phía trên ô nhập.
         Có thể thay thành ScrollView/RecyclerView nếu chat dài.
         ================================= -->
    <ScrollView
        android:id="@+id/scroll_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/input_container"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <!--
        Dùng LinearLayout (vertical) bên trong ScrollView
        hoặc bạn có thể chỉ cần 1 TextView nếu chỉ hiển thị 1 đoạn text.
        Ở ví dụ này, mình vẫn dùng 1 TextView đơn giản.
    -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/chatResult"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Bot: Xin chào!"
                android:textColor="#000000"
                android:textSize="16sp"
                android:background="#DDFFFFFF"
                android:padding="12dp"
                android:layout_marginBottom="8dp"
                android:scrollbars="vertical"
                android:overScrollMode="always" />

        </LinearLayout>
    </ScrollView>

    <!-- ================================
             2. Container (ô nhập + nút gửi) nằm dính đáy
             ================================= -->
    <LinearLayout
        android:id="@+id/input_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:background="#F0FFFFFF">

        <!-- 2.1. Ô nhập tin nhắn -->
        <EditText
            android:id="@+id/inputMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Nhập câu hỏi..."
            android:padding="12dp"
            android:background="@android:drawable/edit_text"
            android:textColor="#000000"
            android:textSize="14sp"
            android:maxLines="1"
            android:imeOptions="actionSend"
            android:inputType="text" />

        <!-- 2.2. Nút gửi (icon mũi tên) -->
        <ImageButton
            android:id="@+id/sendButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Send"
            android:padding="8dp"
            android:src="@drawable/ic_send"
            app:tint="#2196F3" />
    </LinearLayout>
</RelativeLayout>
